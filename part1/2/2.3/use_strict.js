// new fetures are introduced from ECMAscript5 that are not backward compatible
// so they are disabled by default
// by writing "use strict" at the top of the program will enable those features

"use strict";

// it will give error for in cases when it whould have not if strict mode was not enabled
// Example:
x = 5; // variable is not declared