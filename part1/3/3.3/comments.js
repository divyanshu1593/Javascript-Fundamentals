// following comment format should be followed to explain what a function returns and what parameters it takes
// this is known as JSDoc comment

/**
 * 
 * @param {number} length 
 * @param {number} width 
 * @returns {number} perimeter of a rectangle
 */
function perimeter(length, width){
    return 2 * (length + width);
}